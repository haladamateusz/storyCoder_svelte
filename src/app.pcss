@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

html,
body {
	@apply h-full overflow-hidden;
}


html.dark .logo {
    /* to black */
    filter: brightness(0) invert(1);
    /* or to blue */

}

html.dark .icon {
    filter: brightness(0.15) invert(1);
}
